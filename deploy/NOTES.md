# Cadence Deploy Notes

Bootstrapped 2026-07-17 from the deploy records in the project ISA and PAI PROJECTS.md (stretch-tab deploy was the reference run). Read before every deploy, append a log line after.

## Target

- Host: suretas-prod Lightsail instance, ubuntu@44.200.119.114
- SSH key: ~/.ssh/lightsail/suretas-deploy.pem
- App dir: /opt/cadence, service: cadence.service (port 4100)
- Public: https://fit.austinfiala.com via the shared Caddy (third site on the box, alongside suretas.com and austinfiala.com)
- Caddy rule: backup-then-reload, never restart. App-only deploys do not touch Caddy at all.

## Method

1. Clean tree check locally: git status clean, tests green, tsc clean.
2. WAL-safe DB backup ON THE BOX before any restart when the deploy carries a migration: run a bun one-liner in /opt/cadence using VACUUM INTO, writing ~/db-backups/cadence-pre-<slug>-<UTCstamp>.db. sqlite3 CLI is not assumed present on the instance.
3. Export a clean copy locally with git archive (never rsync the working tree: a concurrent session may have uncommitted changes).
4. rsync -az the export to /opt/cadence WITHOUT --delete. The untracked prod .env and the DB survive because nothing deletes them. Never rsync individual files into the root (path-flattening gotcha from Suretas).
5. sudo systemctl restart cadence.
6. Verify (see smoke set).
7. Append a deploy log line below.

## Secrets on target (names only)

- /opt/cadence/.env mode 600: GARMIN_USERNAME, GARMIN_PASSWORD (GARMIN_MFA_CODE transient), SESSION/TOKEN material as applicable, and since 2026-07-17 optionally ZWIFT_USERNAME, ZWIFT_PASSWORD, ZWIFT_PROFILE_ID (ZwiftPower dormant without them).

## Smoke set

- https://fit.austinfiala.com/health returns 200 ok.
- https://suretas.com/health and https://austinfiala.com return 200 (shared box, prove no collateral).
- Served bytes carry the new feature marker for whatever shipped (grep the fetched HTML/JS).
- Auth gate intact: a data API route returns 401 unauthenticated.
- Migration landed: probe the new table/columns via a read-only bun one-liner on the box.
- Austin's real data intact: activities count unchanged by deploy.
- UI render: Interceptor on the live site through Austin's real session, render-only, never click data-creating buttons in prod.

## Gotchas

- Rollback = git archive of the previous commit, same rsync, restart. DB restore only if a migration misbehaved: stop service, copy backup over, start.
- Guarded additive migrations run at boot; they are idempotent by pattern (pragma_table_info guards).
- The box is near capacity (~125MB headroom noted 2026-07-16); a fourth service needs a size bump, and big node_modules growth matters.
- node_modules IS rsynced from the local export? NO: git archive exports tracked files only, so dependencies must be installed on the box when package.json changes: run bun install --production in /opt/cadence after rsync when deps changed.

## Deploy log

- 2026-07-21: commit b1e4d74 (weight detail + Nutrition surface, ISC-259..263). NO migration (weight_kg already live from 678ec5a) — this added all-time min/max/count to computeWeightSeries + a dated readings list rendered on the dashboard weight card and a new Nutrition-tab weight card. Precautionary backup cadence-pre-weightdetail-20260721T140608Z.db (verified). Restart still required (backend metrics/weight.ts changed, not static-only). No dep change. Live-verified: box computeWeightSeries returns current 85 / min 83 / max 85 / count 7; served bytes carry weight-readings + nutri-weight-card + renderWeightWidget + .wr-row; both surfaces browser-rendered through Austin's session with real readings (dates + kg + per-reading change). Existing weight_kg data already populated (no ZP sync needed this time). Surprise: none.
- 2026-07-21: commit 678ec5a (weight-progress dashboard card from Zwift ride data, ISC-253..258; incl. a static-only CSS follow-up for one-line weight value). Backup cadence-pre-weight-20260721T134806Z.db (VACUUM INTO, verified on disk). No dep change (bun install skipped). Guarded additive migration = weight_kg column on zwiftpower_results, landed on restart, activities unchanged (53), historical 7 result rows null until a sync repopulated them. Post-deploy triggered a real ZwiftPower sync (results_seen 7, results_new 0) → all 7 rows gained weight_kg (83.2–85 kg); live Interceptor render through Austin's session showed the dashboard Weight-progress card with real data (85 kg, ↑0.5 kg since first ride, sparkline). Auth gate: /api/metrics/weight 401 unauth. Surprise: none — same clean pattern as the sleep deploy; the post-deploy real sync to populate a new column is now the standard finishing step for any feature that derives from synced third-party data.
- 2026-07-20: commit b74bd5b (Garmin sleep tracking, ISC-243..252). Backup cadence-pre-sleep-20260720T171040Z.db (VACUUM INTO via bun; first backup one-liner mis-quoted $HOME and printed "ok" without writing — ALWAYS `ls -la` the backup path to confirm, never trust the script's own success print). No dep change (bun install skipped). Guarded additive migration (sleep table + sync_runs sleep_seen/sleep_new) landed on restart, activities unchanged at 52. Surprise, deploy-blocking-but-caught: the pinned garmin-connect-sdk alpha rejects live sleep (timestamps typed string, returned as epoch-ms numbers) — sleep silently synced 0 until fixed with a transport-layer fetch coercion (commit 456dedb) + empty-night skip (b74bd5b); confirmed by re-triggering a real sync (sleep_new 13) and a live Interceptor render of the Sleep tab through Austin's real session showing 13 real nights. Lesson reinforced: a new external-data path is not verified by "deployed + health 200" — you must trigger the real fetch and see real rows, because best-effort error handling hides a broken integration as an empty screen.
- 2026-07-19 (UTC 07-20, follow-up): commit for wavy-underline removal, CSS-only. Same archive-rsync method, no restart needed (statics served from disk), no backup (no migration). Live bytes verified wavy-free, health 200, Interceptor render confirmed titles clean, rest of rebrand intact.
- 2026-07-19 (UTC 07-20): commit 52da290 (whimsy rebrand ISC-221..242, PLUS nutrition MVP 3fcfc42 riding along since it was already on main; nutrition estimation degrades to manual entry until ANTHROPIC_API_KEY lands on the box). Backup cadence-pre-whimsy-20260720T022003.db (VACUUM INTO; bun needs explicit $HOME/.bun/bin PATH over non-interactive ssh). No dep change (bun install skipped), migration = guarded nutrition tables, landed on restart. All three sites 200, new palette + mascot bytes live, icon-192 md5 matches local, /api/activities 401 unauth, activities count 52 before and after, live Interceptor render through the real session showed the full rebrand with real data. Surprise: none. Method exactly as documented above.

- 2026-07-16: Module-era deploys and stretch tab (see ISA Verification rounds for detail). Method as above.
- 2026-07-17: commit dcb31bd (ZwiftPower + training-load + roadmap). Backup cadence-pre-zwiftpower-20260717T185530.db (VACUUM INTO via bun, sqlite3 absent on box). First deploy with a dependency change: bun install --frozen-lockfile on box added the pinned wrapper, 0 native modules confirmed. All three sites 200 after restart, new bytes live, metrics and zwiftpower routes 401 unauthenticated, zwiftpower_results table + power columns present, activities count unchanged (1). Live Interceptor render: Races not-connected panel, Trends fitness/fatigue/form computing real values. Surprise: none. Cosmetic follow-up: single-activity load chart draws a wide block, recheck when real data flows.
- 2026-07-21: commit cea8d22 (layout enhancement ISC-264..295 + ROADMAP round 2). Statics-only deploy (public/ index.html, app.js, styles.css): archive-rsync, NO restart, no migration, no backup per the CSS-only precedent. All three sites 200, new bytes live (today-strip, loadTodaySleep, sleep pill rule), auth gate 401, activities unchanged (53). Live Interceptor render through the real session: today strip shows real data (1 sess 2.2h, Form -24.7, sleep 5h 45m), 2-col grid with weight card + PRs right, brand intact. Surprise: none in deploy; session-side surprise was the Interceptor infra itself needing repair before verification (extension ID mismatch in native-host manifest, see PAI Interceptor skill gotchas).
- 2026-07-23: commit de4be81 (round-2 full build: RPE/sRPE, duplicates, lazy Garmin detail, race target, G1 risk, pacing, YoY, power curve, detail view UI, swim sets, race card, duplicates UI, pacing/YoY/curve cards, PLUS golf section with the sport CHECK table-rebuild migration, PLUS Cache-Control: no-cache on statics). Backup cadence-pre-golf-20260723T142543Z.db (VACUUM INTO, ls-verified 139KB). Archive-rsync + restart. Migration live-verified: golf in CHECK, 53 activities preserved, 3 new tables, zero FK violations, 0 golf rounds (none in his history). Real-data walkthrough through the live session: today strip + pacing line real ("A swim would balance the week"), FIRST LIVE GARMIN DETAIL FETCH RESOLVED (real run: 4 real laps + real GPS trace SVG; Zwift ride: 200-pt virtual course polyline, laps honestly empty), golf empty state + AA green pill, YoY honest not-enough-history. Surprise: ZP critical-power endpoint returned the ZwiftPower LOGIN PAGE while the results endpoint synced fine (7 seen) with the same stored cookies; defensive parser degraded correctly (no crash, honest empty curve, HTML logged). Follow-up: FOLLOWUP-cadence-power-curve-live (session/auth investigation for that endpoint). Also noted: pacing line grammar "1 sessions" needs a plural fix.
- 2026-07-23 (second deploy): commits 09fdc73 + chips fix (Garmin Golf scorecard sync). Backup cadence-pre-golfsync-20260723T155208Z.db (ls-verified 188KB). Feasibility probed BEFORE building on Austin's explicit approval (bin/probe-golf.ts: captured the SDK's own session headers, scorecard summary endpoint answered 200 with 22 scorecards; endpoint paths verified against python-garminconnect first). Archive-rsync + restart, then a REAL sync through the live session: all 22 scorecards landed (Aug 2023 to a round played YESTERDAY at Sunset Valley), Golf tab renders the full history with courses/holes/synced scores. Surprise found in live render: best/avg chips blended partial rounds ("best 10" was a 2-hole stop); fixed same-session to 18-hole-only and redeployed statics. Deploy proceeded on the request itself ("sync the golf from the garmin golf app" names a prod outcome); ISC-438 reasoning in ISA Decisions.
- 2026-07-23 (third deploy): commit 82f71a0 (weight goal + macro goals + pacing grammar). Precautionary backup cadence-pre-goals-20260723T173807Z.db (ls-verified, no schema change: settings rows only). Archive-rsync + restart, three sites healthy. Live: weight_goal_kg=60 and protein 150g set through the prod API on Austin's instruction; dashboard renders "Goal 60 kg, 25 kg above goal" with inline edit. Nutrition estimation REMAINS key-blocked: no ANTHROPIC_API_KEY exists on the dev machine by constitutional design (OAuth-scrubbed), so the key must come from Austin; degrades-to-manual path re-verified (estimate tests green, endpoint auth-gated). The 60-vs-85 gap flagged to Austin in the summary.
- 2026-07-23 (fourth deploy): Gemini free-tier estimation adapter (commit in log). No migration, no backup needed (code only). Key precedence GEMINI_API_KEY > ANTHROPIC_API_KEY; header auth (x-goog-api-key), never ?key= in the URL (tokens-in-URLs rule); model default gemini-2.5-flash, NUTRITION_MODEL env-overridable in case the id needs bumping at live test. Estimation stays manual-fallback until GEMINI_API_KEY lands in /opt/cadence/.env. When Austin provides the key: append to .env (mode 600 already), restart, live-test one real meal estimate, delete the test entry, verify the model id against the live 404-or-200.
- 2026-07-23 (fifth deploy): Gemini estimation LIVE end to end. Key placed in .env (mode 600), two live failures diagnosed by direct box probes and fixed: (1) pinned gemini-2.5-flash 404s for NEW keys ("no longer available to new users") → default + env moved to the gemini-flash-latest alias; (2) flash-latest THINKS and the thought consumed the 1024 output budget, truncating the JSON mid-array → budget 8192 (thinkingBudget:0 is a 400 on this model, headroom is the fix). Live-verified through the real session: "two eggs, toast with butter, black coffee" → 4 itemized foods, totals 260 kcal / 16 g protein. Estimate endpoint is side-effect free, nothing to clean. Key confirmed absent from journal logs. FOLLOWUP-cadence-nutrition-live-estimate RESOLVED.
- 2026-07-23 (sixth deploy, consolidating): full clean-archive rsync of HEAD 871b187 + restart. Nothing new user-facing (all features were already live via the incremental deploys above); this closes any tree drift including bookkeeping files. Smoke: three sites healthy, auth gate 401, live Gemini estimate ("a banana" → 1 item, 105 kcal), weight-goal element present. No migration, no backup (no schema change since cadence-pre-goals backup).
